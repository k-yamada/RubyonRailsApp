<!-- メインページ。社員名と番号を表示し、名をクリックでページ遷移 -->
<h1>社員名簿</h1>

<ul>
  <% @projects.each do |project| %>

  <li>
    <%= "No." %>
    <%= project.stuffnum %>
    <%= link_to project.title, project_path(project.id) %>
    <%= link_to "[更新]", edit_project_path(project.id) %>
    <%= link_to "[削除]", project_path(project.id),
        method: :delete, data: { confirm: "本当に削除してもよろしいですか？" } %>
  </li>

  <% end %>
</ul>

<h5>
  <%= link_to "Add New Member", new_project_path %>
</h5>
